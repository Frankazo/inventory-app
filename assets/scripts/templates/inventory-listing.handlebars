<div class="col mb-4">
  <div class="card text-center h-100">
    <div class="card-body">
      <h5 class="card-title">Blank Inventory</h5>
      <p class="card-text">PlaceHolder</p>
      <form id="create-btn" class="col lg-2">
        <input type="text" name="inventory[reference]" placeholder="Reference">
        <input type="Date" name="inventory[date]">
        <input class="btn btn-primary create-btn" type="submit" value="Create New">
      </form>
    </div>
  </div>
</div>
{{#each inventories as |inventory|}}
  {{!--{{log book}}--}}
  <div data-id="{{inventory.id}}" class="col mb-4">
    <div class="card text-center h-100">
      <div class="card-header">
        {{#if inventory.reference}}
          {{inventory.reference}}
        {{else}}
          No Reference provided.
        {{/if}}
      </div>
      <div class="card-body">
        <h5 class="card-title">
           {{#if inventory.reference}}
              {{inventory.reference}}
            {{else}}
              No Reference provided.
            {{/if}}
        </h5>
        <!-- <p class="card-text">Placeholder for future cards</p> -->
        <!-- <a href="#" class="btn btn-primary">Inventory</a> -->
        <button data-id="{{inventory.id}}" type="button" name="Remove" class="btn btn-primary">Inventory</button>
      </div>
      <div class="card-footer text-muted">
        {{#if inventory.date}}
          {{inventory.date}}
        {{else}}
          No Date provided.
        {{/if}}
      </div>
    </div>
  </div>
{{/each}}
